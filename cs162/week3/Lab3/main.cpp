#include <iostream>
#include "Die.hpp"
#include "LoadedDie.hpp"

int calcRunningAvg(int total, int turn) {

}

int main() {
    Die die1(6);
    LoadedDie die2(6);

    // std::cout << "die1 roll() = " << die1.roll() << std::endl;
    // std::cout << "die2 roll() = " << die2.roll() << std::endl;
    int turns = 0;
    double playerOneRunningAverage = 0;
    double playerTwoRunningAverage = 0;
    while(turns < 10) {
        
    }

    return 0;
}